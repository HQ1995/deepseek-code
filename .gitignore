# Secrets
.env

# Session / subagent byte-for-byte logs
.subagent-session.jsonl
*.subagent-session.jsonl

# Node / bridge build artifacts (no slash: CI links node_modules to a runtime)
node_modules
lib/
*.tsbuildinfo
pnpm-debug.log
.pnpm-store/

# Vendored grok-build Rust target (also covered by its nested .gitignore)
third_party/grok-build/target/

# OS / editor
.DS_Store
.idea
.vscode/
dist/
