# Peer deps (cordis/schemastery and every @deepseek-ai/dsh-*) resolve from the
# built deepseek-harness tree at runtime through the profile fallback, not from
# npm. Do not let a standalone bridge build pull registry copies of them.
auto-install-peers=false
