#!/usr/bin/env bash
# Fallback dsh launcher for dscode. Real installs normally have dsh on PATH or
# set DSH_BIN; this is only linked when no dsh is reachable. It uses the exact
# npm version this dscode release was tested against.
exec npx --yes @deepseek-ai/dsh@0.1.0-rc.8 "$@"
